{
  "name": "circuit-json-to-connectivity-map",
  "repository": {
    "type": "git",
    "url": "https://github.com/tscircuit/circuit-json-to-connectivity-map.git"
  },
  "main": "dist/index.js",
  "type": "module",
  "version": "0.0.30",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup-node src/index.ts --format esm --sourcemap --dts",
    "format:check": "biome format .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "^1.3.4",
    "circuit-json": "0.0.460",
    "tsup": "^8.2.4"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@tscircuit/math-utils": "^0.0.9"
  }
}
